# ----*---- emacs ----*----
# 
# This section must come after the tex section, and probably after the
# ssh section also.
#
# Start emacs if not done.

EMACS="/home/tiago/src/emac/build/src/emacs"

{ emacsclient -e '(message "Contacting home.")' || \
  ${EMACS} --daemon
} 2>/dev/null 1>&2 &
