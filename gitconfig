# kept in homegit, linked from ~/.gitconfig

[user]
	name = Tiago Saboga
	email = tiagosaboga@gmail.com
[core]
        excludesfile = /home/tiago/etc/gitexcludes
[diff]
[color]
	ui = true
[credential]
	helper = cache --timeout=3600
[alias]
	permission-reset = !git diff -p -R | grep -E \"^(diff|(old|new) mode)\" | git apply
